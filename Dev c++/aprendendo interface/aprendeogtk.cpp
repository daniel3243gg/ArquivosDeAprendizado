//foo.cpp
#incluir <gtkmm.h>

int principal(int argc, char** argv)
{
        aplicativo automático = Gtk::Application::create(argc, argv);

        Gtk::Janela janela;
        janela.set_default_size(600.400);

        return app->executar(janela);
}
